package it.polimi.ingsw.model;

/**
 * Enum class that represents the state of a coordinates pair on a player's board.
 */
public enum State {
        AVAILABLE,
        UNAVAILABLE,
        OCCUPIED,
        UNPLAYED
}
